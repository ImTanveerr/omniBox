import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "FoodieExpress - Fast Food Delivery",
  description: "Get your favorite food delivered fast with FoodieExpress. Fresh ingredients, quick delivery, great taste.",
  keywords: "food delivery, restaurant, takeout, fast food, online ordering",
  authors: [{ name: "FoodieExpress Team" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
